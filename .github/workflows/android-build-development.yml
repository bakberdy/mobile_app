name: android-build-development

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Check required secrets
        env:
          CFG_API_URL: ${{ secrets.DEVELOPMENT_API_URL }}
          CFG_ENVIRONMENT: ${{ secrets.DEVELOPMENT_ENVIRONMENT }}
        run: |
          set -euo pipefail
          rc=0
          if [ -z "${CFG_API_URL}" ]; then echo "::error::Secret DEVELOPMENT_API_URL is missing or empty."; rc=1; fi
          if [ -z "${CFG_ENVIRONMENT}" ]; then echo "::error::Secret DEVELOPMENT_ENVIRONMENT is missing or empty."; rc=1; fi
          if [ "${rc}" -ne 0 ]; then
            echo "::error::Add repository secrets under Settings → Secrets and variables → Actions."
            exit 1
          fi

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/*.gradle*', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Flutter clean, pub get
        run: |
          set -euo pipefail
          flutter clean
          flutter pub get

      - name: Parse version from pubspec.yaml
        id: pubspec-version
        run: |
          set -euo pipefail
          LINE=$(grep -E '^version:' pubspec.yaml | head -1)
          RAW=$(printf '%s' "$LINE" | sed -E 's/^version:[[:space:]]*//' | tr -d "'\"")
          if [[ "$RAW" == *+* ]]; then
            echo "version_name=${RAW%+*}" >> "$GITHUB_OUTPUT"
            echo "version_code=${RAW#*+}" >> "$GITHUB_OUTPUT"
          else
            echo "version_name=${RAW}" >> "$GITHUB_OUTPUT"
            echo "version_code=0" >> "$GITHUB_OUTPUT"
          fi

      - name: Build APK
        env:
          CFG_API_URL: ${{ secrets.DEVELOPMENT_API_URL }}
          CFG_ENVIRONMENT: ${{ secrets.DEVELOPMENT_ENVIRONMENT }}
        run: |
          flutter build apk --release --flavor development \
            --dart-define=API_URL="$CFG_API_URL" \
            --dart-define=ENVIRONMENT="$CFG_ENVIRONMENT"

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: android-development-release-${{ steps.pubspec-version.outputs.version_name }}-${{ steps.pubspec-version.outputs.version_code }}
          path: build/app/outputs/flutter-apk/app-development-release.apk
          if-no-files-found: error
